type Review {
    id: ID!
    rating: Int!
    desc: String!
    createdAt: String!
    updatedAt: String!
    member: Member!
}

type ReviewsConnection {
    # totalCount: Int!
    edges: [ReviewEdge!]!
    pageInfo: PageInfo!
}

type ReviewEdge {
    node: Review
    cursor: ID!
}
