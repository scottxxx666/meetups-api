schema {
    query: Query
}
type Query {
    member(id: ID!): Member
    organization(id: ID!): Organization
    meetup(id: ID!): Meetup
    review(id: ID!): Review
    reviews(meetupId: ID!): [Review!]!
}